# Architecture Toolbox (`atb`)

Pluggable, language-agnostic tool for analyzing the architecture of a code repository.

```bash
sbt "cli/run serve /path/to/java-repo"
sbt "cli/run export /path/to/java-repo --format json --out graph.json"
```

See [TASKS.md](TASKS.md) for the full specification.
